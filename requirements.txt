# Runtime dependencies for the MaskTerial Trainer GUI.
#
# `pip install -r requirements.txt` installs everything the desktop app
# itself needs. Training runners additionally need PyTorch and the
# MaskTerial package — see the README for those (PyTorch's CUDA wheels
# need a specific index URL, so they're not pinned here).

PySide6>=6.6
numpy
Pillow
opencv-python
matplotlib>=3.5
pycocotools>=2.0
scikit-learn
