# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
*.eggs/
build/
dist/
develop-eggs/
downloads/
eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.installed.cfg
MANIFEST

# --- Virtual environments ---
.venv/
venv/
env/
ENV/
.python-version

# --- Test / coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
nosetests.xml
coverage.xml
*.cover
*.py,cover
.cache/
.hypothesis/

# --- Type checkers / linters ---
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.pyright/
.pytype/

# --- Editors / IDEs ---
.vscode/
.idea/
*.iml
*.sublime-project
*.sublime-workspace
*~
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Jupyter ---
.ipynb_checkpoints/

# --- Local user state from MaskTerial Trainer ---
# Per-user config + cached pretrained weights live in ~/.maskterial_trainer/,
# but if a developer ever symlinks or generates one inside the repo, ignore it.
.maskterial_trainer/

# --- Project data created at runtime ---
# These appear inside any project folder a user opens, but if the developer
# ever stages a demo project inside the repo, don't commit its outputs.
outputs/
coco/
splits/
instance_masks/
semantic_masks/
*.lnk

# --- OS / misc ---
*.log
*.tmp

# Bundled MaskTerial source tree (kept side-by-side during development but
# not part of this package). Remove this line if you decide to vendor it.
MaskTerial_Repo/
